package aplicacion.logica.ventas;

public interface PurchaseValidator {
    // boolean validarCarrito(CarritoCompras carritoCompras);
    // boolean libretaCompro(Libreta libreta);
    // boolean tarjetonCompro(Tarjeton tarjeton, Set~Producto~ producto);
    // boolean recetaCompro(Receta receta);
    // boolean validarReceta(Receta receta);
    // boolean validarLibretas(Libreta libreta);
    // boolean validarTarjeton(Tarjeton tarjeton);
}
